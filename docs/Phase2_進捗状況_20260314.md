# Phase 2 (PDFデータ化 & 一括登録機能) 進捗・残タスクまとめ

本ドキュメントは、2026年3月15日時点での「AIによるPDFレシート/明細の一括登録機能」に関する実装状況をまとめたものです。

---

## 1. 実装完了した対応内容

### バックエンド (FastAPI)
- [x] **スキーマ定義の厳密化:** `source_type` を `Literal["manual", "ai_pdf", "csv_import"]` に制限
- [x] **AIパッケージ導入:** `google-genai` SDK (Gemini 2.5 Flash) へ移行済み
- [x] **Gemini連携ロジック (`ai_service.py`):** PDF → File API → JSON抽出 → マスタマッピング
- [x] **エンドポイント:** `POST /api/v1/receipts/analyze` 
- [x] **サーバー復旧:** 消失していた `api.py` を復元

### フロントエンド (Vue 3 + Vite)
- [x] **状態管理 (`useImportStore.ts`):** Piniaストア
- [x] **データ化確認ビュー (`ImportVerifyView.vue`):** 2ペインUI (左:PDFプレビュー / 右:インライン編集テーブル)
- [x] **ダッシュボード連携:** 「PDF一括登録」ボタン追加、自動遷移
- [x] **ルーティング:** `/import` ルート

## 2. E2Eテスト結果 ✅
```
STEP 1: PDF読み込み        → 498 bytes
STEP 2: Gemini 2.5 Flash解析 → 1件抽出 (date=2026-03-14, amount=1400, payer=俊介)
STEP 3: DB一括登録         → Transaction ID作成成功
STEP 4: DB検証             → FOUND, 全フィールド一致
E2E TEST PASSED!
```

## 3. 修正事項
- `google-generativeai` → `google-genai` へSDK移行（旧パッケージ非推奨）
- モデル名 `gemini-1.5-flash` → `gemini-2.5-flash` へ更新（旧モデル404エラー）

## 4. 残作業（ユーザー確認待ち）
- [ ] **ブラウザでの実機テスト:** ローカルブラウザから実際のPDFをアップロードして画面遷移・テーブル表示の確認
- [ ] **実際のレシートPDFでのAI精度検証:** プロンプトチューニングが必要か判断
